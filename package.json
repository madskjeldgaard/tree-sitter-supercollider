{
  "name": "tree-sitter-supercollider",
  "version": "0.2.2",
  "description": "Tree sitter parser for the SuperCollider programming language",
  "main": "bindings/node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mads kjeldgaard",
  "license": "ISC",
  "dependencies": {
    "nan": "^2.14.2"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.18.0"
  },
  "tree-sitter": [
    {
      "scope": "source.scd",
      "file-types": [
        "scd",
        "sc"
      ]
    }
  ]
}
